{
  "version": 1,
  "next_id": 8,
  "todos": [
    {
      "id": 2,
      "description": "Display the tags on the photo",
      "context": "The center panel is a zoomable image widget (ui.Viewer, wrapped by MainWindow into a Border with the ActionPanel on top and stacked with the grid viewer), and today nothing is painted over the photo: per the standing UI Layout decision in CLAUDE.md, favorite/color state is surfaced only through the toolbar buttons, the context menu and the file list, all driven from Application.showPhoto -> updateActionStates. Tags (title + keywords) are visible only inside the modal Tags dialog, which re-reads them per photo through exifService.GetStockInfo from the XMP sidecar/JPEG packet; there is no in-memory tag cache yet (todo #1). Where the tags go relative to the \"nothing over the photo\" rule is not decided yet.",
      "docs": [
        "internal/gui/ui/viewer.go",
        "internal/gui/ui/window.go",
        "internal/gui/app/navigation.go",
        "internal/gui/app/actions.go",
        "internal/gui/ui/toolbar.go",
        "internal/gui/ui/tags_dialog.go",
        "internal/core/imaging/exif_tags.go",
        "CLAUDE.md"
      ],
      "depends_on": [],
      "status": "open",
      "branch": null,
      "created_at": "2026-08-23T13:45:33Z",
      "updated_at": "2026-08-23T13:53:45Z",
      "plan": {
        "created_at": "2026-08-23T13:53:45Z",
        "revised_at": null,
        "base_commit": "4fce1b4",
        "summary": "Surface the current photo's stock tags (title + keywords) in the center panel without breaking the standing \"nothing is drawn over the photo\" rule: add a new `ui.TagBar` strip that MainWindow places in the bottom slot of the center Border (below the zoomable viewer), fed from `Application.showPhoto` by an asynchronous, debounced `exifService.GetStockInfo` read that is applied only while the photo it was started for is still the current one. The bar collapses to zero height when the photo has no tags, is suppressed in grid mode, stays in sync with edits made in the Tags dialog (generate / save / close), and can be toggled off with a new `I` shortcut whose state is remembered in preferences. No core package changes are needed: `GetStockInfo` already returns exactly the data required and is safe to call from a worker goroutine (it takes the service RLock); every widget touch is wrapped in `fyne.Do` per the project's threading rules. The work is independent of todo #1 (the tag cache), which would later only replace the read behind `loadTagsFor` with a cache lookup.",
        "steps": [
          {
            "title": "Add the TagBar widget",
            "details": "New `ui.TagBar` in `internal/gui/ui/tagbar.go`, modelled on the small composite widgets already in the package (Notifier, ActionPanel): `NewTagBar() *TagBar`, `Container() *fyne.Container`, `SetTags(model.Tags)`, `Clear()`, `SetEnabled(bool)` (user toggle) and `SetSuppressed(bool)` (grid mode). Internals: a padded VBox with a bold title `widget.Label` and a keywords `widget.Label`, both `Truncation = fyne.TextTruncateEllipsis` so the strip keeps a fixed two-line height whatever the tags are; keywords rendered via `model.Tags.KeywordLine()`. Visibility is derived, not set directly: the container shows only when `enabled && !suppressed && !tags.IsEmpty()`; a single `refresh()` recomputes it after every setter. Empty title or empty keyword list hides just that label. Tests in `internal/gui/ui/tagbar_test.go` following `tags_dialog_test.go` (fyne `test.NewTempWindow`, testify, non-parallel): tags shown, empty tags hide the bar, `Clear` hides it, disabled bar stays hidden after `SetTags`, re-enabling brings it back, suppression wins over enabled.",
            "files": [
              "internal/gui/ui/tagbar.go",
              "internal/gui/ui/tagbar_test.go"
            ]
          },
          {
            "title": "Place the bar under the photo in MainWindow",
            "details": "Extend `NewMainWindow` with a `*TagBar` parameter, store it on `MainWindow`, and in `build()` change the center panel to `container.NewBorder(mw.actionPanel.Container(), mw.tagBar.Container(), nil, nil, mw.viewStack)` so the strip sits below the viewer and never overlaps the image (the notifier stack stays as it is). In `SetGridMode` call `mw.tagBar.SetSuppressed(grid)` next to the existing action panel show/hide. Add thin forwarders used by the app layer: `SetPhotoTags(model.Tags)`, `ClearPhotoTags()`, `SetTagsVisible(bool)`. Update the single construction site in `internal/gui/app/app.go` (`ui.NewTagBar()` built next to `ui.NewNotifier()` and passed in).",
            "files": [
              "internal/gui/ui/window.go",
              "internal/gui/app/app.go"
            ]
          },
          {
            "title": "Load and show the tags of the current photo",
            "details": "New `internal/gui/app/tags_view.go` holding the read path. `Application` gains two fields (`tagGeneration int`, `tagTimer *time.Timer`). `showTagsFor(photo model.Photo)`: increments the generation, clears the bar immediately (the strip must never describe the previous photo, same reasoning as `clearActionStates`), stops any pending timer and starts `time.AfterFunc(tagReadDelay, ...)` with ~200ms so arrow-key scrolling does not open every skipped JPEG; the timer goroutine calls `a.exifService.GetStockInfo(photo)` and then `fyne.Do` to check `a.tagGeneration == captured` plus `navigator.Current().ImagePath == photo.ImagePath` before `a.mainWindow.SetPhotoTags(info.Tags)` (stale results dropped, read errors only logged). `clearTags()` bumps the generation, stops the timer and clears the bar. Call `showTagsFor` from `Application.showPhoto` in `internal/gui/app/navigation.go` (both on the success and the load-failure branch, next to `updateActionStates`) and `clearTags` from `clearViewer` in `internal/gui/app/actions.go`. Grid mode needs nothing extra.",
            "files": [
              "internal/gui/app/tags_view.go",
              "internal/gui/app/navigation.go",
              "internal/gui/app/actions.go"
            ]
          },
          {
            "title": "Keep the bar in sync with the Tags dialog",
            "details": "In `internal/gui/app/actions.go` push the dialog's tags into the bar instead of re-reading the file: add `setTagsIfCurrent(photo model.Photo, tags model.Tags)` to `internal/gui/app/tags_view.go` (bumps the generation so an in-flight read cannot overwrite the newer tags, then sets the bar only while `navigator.Current()` is still that photo) and call it from `tagsSession.generate` right after `s.dialog.SetTags(generated)`, from `tagsSession.saveJPEG`/`saveSidecar` success and from `tagsSession.close` after the final `saveSidecar`, so an edit made in the dialog — including clearing both fields — is visible under the photo the moment the dialog goes away. `prefill` also calls it, which gives the bar the freshly read tags when the dialog was opened before the debounced read finished.",
            "files": [
              "internal/gui/app/actions.go",
              "internal/gui/app/tags_view.go"
            ]
          },
          {
            "title": "Toggle shortcut, help entry and documentation",
            "details": "Add `OnToggleTags` to `ui.ShortcutCallbacks` and bind it to `glfw.KeyI` in the scancode map in `internal/gui/ui/shortcuts.go` (I is free: F R G B D C N S 1-4 H Y L Z T are taken). Wire it in `internal/gui/app/app.go` to a handler in `internal/gui/app/tags_view.go` that flips `a.showTags`, calls `a.mainWindow.SetTagsVisible(...)` and persists the flag; guard it with the existing `shortcutsBlocked()`. Persist through `internal/gui/app/settings.go` with a `showTagsKey = \"showTags\"` constant, restored by a new `restoreTagVisibility` called from `Run` before the first directory load, default true. Add `{\"I\", \"Toggle tag strip\"}` to `helpLeft` in `internal/gui/ui/help.go`. Update `CLAUDE.md`: the UI Layout section gets a line saying the tag strip sits below the photo (the favorite/color rule is unchanged), and the Keyboard Shortcuts list gets `I`.",
            "files": [
              "internal/gui/ui/shortcuts.go",
              "internal/gui/app/tags_view.go",
              "internal/gui/app/app.go",
              "internal/gui/app/settings.go",
              "internal/gui/ui/help.go",
              "CLAUDE.md"
            ]
          }
        ],
        "questions": [
          {
            "question": "Placement: assume a strip below the photo (inside the center panel, image never covered) unless you want the tags painted over the bottom of the image the way the notifier toast is — which would need the CLAUDE.md 'nothing is drawn over the photo' rule amended. Affects steps 1-2.",
            "answer": null
          },
          {
            "question": "Content: assume one truncated line for the title and one truncated line for the keywords (fixed height, full text stays in the Tags dialog). Do you instead want the keywords word-wrapped over several lines, or the keyword count only? Affects step 1.",
            "answer": null
          },
          {
            "question": "Toggle: assume the strip can be hidden with `I` and that the choice is remembered between launches, default on. Is `I` acceptable, or should the strip always be on? Affects step 5.",
            "answer": null
          },
          {
            "question": "Photos with no tags: assume the strip collapses completely (no 'no tags' placeholder), so the viewer keeps the full height for untagged folders. Affects step 1.",
            "answer": null
          },
          {
            "question": "Read cost: assume a ~200ms debounce before reading tags during navigation, and that a read error is only logged, not shown as an error toast. Affects step 3.",
            "answer": null
          }
        ],
        "risks": [
          "Every navigation step reads the XMP/EXIF of a file until todo #1 lands a tag cache; the debounce limits it, but a slow network volume can still make the strip lag behind the photo.",
          "The strip appearing and disappearing changes the viewer's height, so a zoomed-in photo is re-laid out (pan is clamped in `zoomWidget.clampPan`, so nothing breaks, but the view shifts). Fixed strip height keeps the shift constant.",
          "`NewMainWindow`'s signature changes; it has one caller (`internal/gui/app/app.go`) but is already six parameters long and grows to seven.",
          "Stale-result handling depends on the generation counter plus the current-path check; getting either wrong shows one photo's tags under another.",
          "`GetStockInfo` returns empty tags and no error for a PNG or a JPEG without EXIF, which is indistinguishable from 'no tags' — acceptable, but it means the strip cannot report an unreadable file."
        ]
      }
    },
    {
      "id": 3,
      "description": "Add a background mode for tag generation",
      "context": "Tag generation (stock title + keywords via the claude CLI in internal/core/tags) runs only as a foreground, per-photo modal flow today: `T` -> Application.handleTags builds a tagsSession whose generate() spawns a goroutine writing into the Tags dialog and an XMP sidecar, with the context cancelled in tagsSession.close when the dialog is dismissed. Existing background machinery it would sit next to: imaging.Provider.LoadFolder (bounded worker pool, generation counters, onLoaded callback), the bulk-operation pattern in handleCopyAll/handleDeleteAll with ui.BulkDialog progress, and ui.Notifier for completion messages; the Fyne threading rules in CLAUDE.md apply to any UI update from a worker goroutine. Related open todos: #1 tag cache, #2 display tags on the photo.",
      "docs": [
        "internal/gui/app/actions.go",
        "internal/core/tags/tagger.go",
        "internal/gui/ui/tags_dialog.go",
        "internal/gui/app/app.go",
        "internal/core/imaging/image_provider.go",
        "internal/gui/ui/bulk_dialog.go",
        "internal/gui/ui/notifier.go",
        "CLAUDE.md"
      ],
      "depends_on": [],
      "status": "open",
      "branch": null,
      "created_at": "2026-08-23T13:47:50Z",
      "updated_at": "2026-08-23T13:53:45Z",
      "plan": {
        "created_at": "2026-08-23T13:53:45Z",
        "revised_at": null,
        "base_commit": "4fce1b4",
        "summary": "Add background (non-modal) tag generation alongside today's per-photo modal flow. A new core `tags.Queue` runs Claude CLI requests on a bounded worker pool with cancellation, de-duplication and progress/result callbacks, mirroring the shape of imaging.Provider.LoadFolder. A new GUI-side controller (internal/gui/app/tags_background.go) owns that queue, applies the persistence policy (XMP sidecar next to the RAW), reports progress and reports the summary through ui.Notifier, always hopping to fyne.Do before touching widgets. Two entry points use it: a new \"Tag All\" button in the file browser bulk bar that tags the currently filtered photos, and the existing Tags dialog, whose in-flight run is handed over to the controller instead of being cancelled when the dialog is dismissed. A small progress row with a Stop button in the left panel shows how far the batch is and cancels it. Help text is updated last.",
        "steps": [
          {
            "title": "Add a bounded tag-generation queue to internal/core/tags",
            "details": "New file internal/core/tags/queue.go with a `Queue` that has no Fyne and no I/O beyond the generator it is given: `NewQueue(generate func(context.Context, Request) (model.Tags, error), workers int) *Queue`, so *Tagger is passed as `tagger.Generate` and tests inject a fake. State under one mutex: pending []Request, active map keyed by Request.Photo.ImagePath (re-enqueueing a queued or running photo is a no-op), running worker count, done/total counters for the current burst, and a ctx/cancel created when the first worker starts. API: `Enqueue(reqs ...Request) int`, `Cancel()`, `Progress() (done, total int)`, `Busy() bool`, and a `Callbacks` struct with `OnResult(Result)` and `OnFinished(Summary)`. Each worker pops under the lock, runs generate with the queue context, fires OnResult, and the last worker fires OnFinished exactly once. Comment that callbacks run on worker goroutines (the CLAUDE.md fyne.Do rule then applies to callers). Also add `var ErrUsageLimit = errors.New(\"usage limit reached\")` in internal/core/tags/tagger.go, wrapped into the usage-limit branch of responseMessage/responseFailure with %w (message text unchanged), so a batch can stop itself. Tests in internal/core/tags/queue_test.go (table/subtest style like tagger_test.go, testify): concurrency never exceeds `workers`, duplicates dropped, Cancel stops pending work and cancels the passed context, OnFinished fires once with the right counts, per-photo results and errors. Extend tagger_test.go with an errors.Is(err, ErrUsageLimit) assertion on the existing usage-limit fixtures.",
            "files": [
              "internal/core/tags/queue.go",
              "internal/core/tags/queue_test.go",
              "internal/core/tags/tagger.go",
              "internal/core/tags/tagger_test.go"
            ]
          },
          {
            "title": "Add the GUI-side background tagging controller and wire it into Application",
            "details": "New file internal/gui/app/tags_background.go holding `backgroundTagger`: it owns a *tags.Queue built over a.tagger.Generate plus the policy the queue must not know about. The `generate` wrapper calls a.tagger.Generate and on success persists: when photo.HasRAW(), imaging.WriteSidecar(model.SidecarPath(photo.RAWPath), tags), mirroring tagsSession.saveSidecar; a photo without a RAW pair is not written to at all and is counted as skipped. `RunNow(req, onResult)` starts a single detached goroutine outside the pool for the Tags dialog (step 4) so an interactive run is never stuck behind a batch, sharing the same persist + notify path. Failures are logged (log.Printf, as handleDeleteAll does); a tags.ErrUsageLimit result cancels the whole batch and shows one warning. Every UI touch goes through fyne.Do because callbacks arrive on queue workers. Wire it in internal/gui/app/app.go: new field `bgTags *backgroundTagger`, constructed in New() (`backgroundTagWorkers = 2`) and given its window/progress hooks in Run() after mainWindow exists; cancel the queue from the window's close/lifecycle hook next to the existing SetOnStarted wiring. Inert but compiles and is reviewable on its own.",
            "files": [
              "internal/gui/app/tags_background.go",
              "internal/gui/app/app.go"
            ]
          },
          {
            "title": "Add the \"Tag All\" bulk entry point over the filtered photos",
            "details": "internal/gui/ui/filebrowser.go: add `OnTagAll func()` to FileBrowserCallbacks and a `tagAllBtn` (theme.DocumentCreateIcon(), the icon the Tags action uses in toolbar.go) into fb.bulkBar next to Delete All / Copy All, under the same visibility rule (updateBulkBarVisibility). internal/gui/ui/bulk_dialog.go: add `NewTagAllDialogContent(count int) (*fyne.Container, *widget.Check)` following NewDeleteAllDialogContent — a bold \"Generate tags for N filtered photos?\" label plus a \"Skip photos that already have tags\" check (checked by default), wrapped in minWidthLayout{width: copyDialogWidth}. internal/gui/app/actions.go: `handleTagAll` next to handleDeleteAll/handleCopyAll — guard on a.gridMode || a.dialogs.anyOpen(), take a.fileBrowser.FilteredPhotos(), show the confirm dialog through a.dialogs.open(dialogTagAll, ...), and on confirm start one goroutine that (a) with the skip box checked drops photos whose exifService.GetStockInfo already returns non-empty tags, (b) builds tags.Request values with the stored `claudePath` preference and no notes, (c) enqueues them via a.bgTags and reports \"Queued N photos for tagging\" (or \"Nothing to tag\") through fyne.Do + ShowNotification. internal/gui/app/dialogs.go: add the `dialogTagAll` kind. internal/gui/app/app.go: pass OnTagAll: a.handleTagAll in the FileBrowserCallbacks literal.",
            "files": [
              "internal/gui/ui/filebrowser.go",
              "internal/gui/ui/bulk_dialog.go",
              "internal/gui/app/actions.go",
              "internal/gui/app/dialogs.go",
              "internal/gui/app/app.go"
            ]
          },
          {
            "title": "Let a Tags dialog run survive the dialog being closed",
            "details": "internal/gui/app/actions.go: route tagsSession.generate through a.bgTags.RunNow instead of its own goroutine + tagger call. The result callback keeps the existing `a.dialogs.isCurrent(s.dialog)` guard, keeps setting the `claudePath` preference on success, and keeps calling s.dialog.SetTags / s.dialog.Fail; the sidecar write for generated tags now belongs to the controller alone, so tagsSession.saveSidecar keeps only its close-time job of persisting user edits (leave its `saved` bookkeeping intact so a run followed by a close does not write the same bytes twice — WriteSidecar already no-ops on identical content). Stop cancelling the run in tagsSession.close: drop the per-session context and the cancel passed to a.dialogs.open, so `Esc` or Close leaves the run going; when the run finishes with the dialog gone, the controller persists the sidecar and shows \"Tags generated for <name>\" (or the failure) through the notifier. Reflect this in internal/gui/ui/tags_dialog.go only if the generating status line should say the run continues in the background (a one-line setStatus text change); extend internal/gui/ui/tags_dialog_test.go if that text is asserted.",
            "files": [
              "internal/gui/app/actions.go",
              "internal/gui/ui/tags_dialog.go",
              "internal/gui/ui/tags_dialog_test.go"
            ]
          },
          {
            "title": "Show background progress with a Stop button in the left panel",
            "details": "New internal/gui/ui/tag_progress.go: a small `TagProgress` type wrapping a widget.ProgressBar plus a label (\"Tagging 3/20\") and a Stop button, hidden by default, with `SetProgress(done, total int)`, `Hide()` and an OnStop callback. internal/gui/ui/filebrowser.go: build one in FileBrowser, add it to the existing `topBars` VBox (below bulkBar so it stays visible whether or not a filter is on), add `OnStopTagging func()` to FileBrowserCallbacks and expose `SetTagProgress(done, total int)` / `HideTagProgress()`. internal/gui/app/tags_background.go: feed OnResult/OnFinished into those two methods inside fyne.Do, and on OnFinished show the summary through the notifier (\"Tagged 18/20 photos (2 failed)\" as a warning when anything failed or was skipped, a plain notification otherwise), mirroring handleCopyAll's wording. internal/gui/app/app.go: pass OnStopTagging: a.bgTags.Cancel. Add a table test in internal/gui/ui/tag_progress_test.go (test.NewTempWindow like tags_dialog_test.go) covering visibility and label text for zero/partial/complete states.",
            "files": [
              "internal/gui/ui/tag_progress.go",
              "internal/gui/ui/tag_progress_test.go",
              "internal/gui/ui/filebrowser.go",
              "internal/gui/app/tags_background.go",
              "internal/gui/app/app.go"
            ]
          },
          {
            "title": "Document the background mode",
            "details": "internal/gui/ui/help.go: extend the Actions section only if a keyboard shortcut is added (see the last question); otherwise no change there. CLAUDE.md: in Features/UI Layout, note that Tag All in the bulk bar generates tags for the filtered photos in the background, that background results are written to the XMP sidecar of the RAW pair, that the Tags dialog no longer cancels a run when closed, and that progress is shown in the left panel with a Stop button. Keep the edit confined to the feature/shortcut documentation.",
            "files": [
              "CLAUDE.md",
              "internal/gui/ui/help.go"
            ]
          }
        ],
        "questions": [
          {
            "question": "Scope check for steps 3 and 4: background mode = (a) bulk tagging of the currently filtered photos plus (b) an interactive run that keeps going after the Tags dialog is closed. Assume both unless told otherwise.",
            "answer": null
          },
          {
            "question": "Step 2: how many concurrent claude CLI runs? Each spawns a process with a 3-minute timeout and consumes subscription quota. Assume 2 workers unless told otherwise; should it be a preference instead of a constant?",
            "answer": null
          },
          {
            "question": "Step 2: a photo without a RAW pair has nowhere to store tags except the JPEG's own XMP packet, which the foreground flow only writes behind an explicit button. Assume background runs skip JPEG-only photos and count them as skipped, rather than writing into the JPEG.",
            "answer": null
          },
          {
            "question": "Step 3: should Tag All skip photos that already carry a title and keywords? Assume yes, with a checkbox in the confirm dialog checked by default.",
            "answer": null
          },
          {
            "question": "Step 3: bulk runs send no Concept/Location/Editorial notes, unlike the dialog. Assume no notes unless the dialog's inputs should be reused for the whole batch.",
            "answer": null
          },
          {
            "question": "Step 2: should a running batch be cancelled when the user opens another folder or quits? Assume it keeps running across folder changes (paths are absolute) and is cancelled only by Stop or app exit.",
            "answer": null
          },
          {
            "question": "Step 2: on a usage-limit error, assume the whole batch is cancelled and one warning is shown, instead of every remaining photo failing the same way.",
            "answer": null
          },
          {
            "question": "Step 6: does background tagging need a keyboard shortcut of its own (e.g. Shift+T for the current photo)? Assume no shortcut for now; button only.",
            "answer": null
          }
        ],
        "risks": [
          "Concurrent claude CLI processes multiply cost and can trip the subscription usage limit quickly; the worker count and the usage-limit abort are the only brakes.",
          "Two writers for the same sidecar: the controller writes generated tags while tagsSession still writes edited tags on close. WriteSidecar is a read-modify-write with no lock (ExifService.access does not cover it), so a close landing exactly on a finishing run can lose one of the two writes.",
          "Detaching the dialog run removes the cancel-on-close guarantee: a user who closes the dialog can no longer stop a run except through the new Stop button; make sure the controller's context is also cancelled on app exit so no claude process outlives the window.",
          "Queue callbacks arrive on worker goroutines and the thread checks are compiled out (CLAUDE.md), so a missing fyne.Do will not warn — it will corrupt the canvas intermittently.",
          "Deleting or moving photos while a batch runs leaves stale requests that fail per photo; noise in the summary counts.",
          "Overlap with open todos #1 (tag cache) and #2 (display tags): the persistence helper added here is the code #1 would move behind a cache, so expect a merge touch-up in internal/gui/app/tags_background.go."
        ]
      }
    },
    {
      "id": 5,
      "description": "Move all OS-dependent code into separate packages under internal",
      "context": "Per-OS/build-tagged code should be pulled out of feature packages into dedicated single-purpose packages under internal, following the existing internal/core/clipboard and internal/core/proc pattern. The trigger is file_created_{darwin,windows,other}.go, added directly inside internal/core/imaging (fileCreated via syscall Birthtimespec / Win32FileAttributeData.CreationTime / ModTime), called from exif_tags.go. The other OS-split site is internal/gui/ui/maximize_{darwin,windows,unix}.go; internal/core/tags/claude_path.go switches on runtime.GOOS rather than build tags, and CLAUDE.md documents the package layout that must stay in sync.",
      "docs": [
        "internal/core/imaging/file_created_darwin.go",
        "internal/core/imaging/file_created_windows.go",
        "internal/core/imaging/file_created_other.go",
        "internal/core/imaging/exif_tags.go",
        "internal/core/clipboard/clipboard_other.go",
        "internal/core/proc/hide_other.go",
        "internal/gui/ui/maximize_darwin.go",
        "CLAUDE.md"
      ],
      "depends_on": [],
      "status": "open",
      "branch": null,
      "created_at": "2026-08-24T10:34:12Z",
      "updated_at": "2026-08-24T11:06:40Z",
      "plan": {
        "created_at": "2026-08-24T11:06:40Z",
        "revised_at": null,
        "base_commit": "4f3a7c9",
        "summary": "Pull the remaining per-OS code out of the feature packages into dedicated single-purpose packages under `internal`, matching the `internal/core/clipboard` and `internal/core/proc` pattern. Three sites move: (1) `fileCreated` in `internal/core/imaging/file_created_{darwin,windows,other}.go` becomes a new `internal/core/filedate` package exporting `Created(path) time.Time`, with only the birth-time extraction kept behind build tags and the `os.Stat`/fallback logic shared; `imaging` (exif_tags.go `withFileDate`, plus the tests in exif_tags_test.go and orientation_test.go that call `fileCreated` directly) switches to it. (2) `internal/gui/ui/maximize_{darwin,windows,unix}.go` plus `maximize_darwin.m` move into a new `internal/gui/nativewin` package exporting `Maximize(fyne.Window)`; `screen.go`/`screen_test.go` must move with them because `maximize_unix.go` uses `screenLayout`/`maximizedContentSize`/`primaryVideoMode`, so `MonitorSize` becomes `nativewin.MonitorSize` and `internal/gui/app/app.go` is updated alongside `internal/gui/ui/window.go`. (3) `internal/core/tags/claude_path.go`, which branches on `runtime.GOOS` rather than build tags, moves wholesale into `internal/core/claudebin` as `Lookup`/`ErrNotFound`, keeping the runtime switch so both the Windows and the POSIX branches stay testable from any host. Each step also updates the package-layout tree in CLAUDE.md, which the project requires to stay in sync. Nothing new is imported into `internal/core` from Fyne, and all three steps keep `go build ./...`, the ubuntu CI build (which is what compiles the `!darwin && !windows` branches) and the mingw Windows cross-build working.",
        "steps": [
          {
            "title": "Extract file creation time into internal/core/filedate",
            "details": "Create `internal/core/filedate` with a shared `created.go`: `Created(path string) time.Time` does the `os.Stat`, returns the zero time on error, calls a per-OS `birthTime(info os.FileInfo) (time.Time, bool)` and falls back to `info.ModTime()` when it reports false. Add `birthtime_darwin.go` (`*syscall.Stat_t` -> `Birthtimespec`), `birthtime_windows.go` (`*syscall.Win32FileAttributeData` -> `CreationTime.Nanoseconds()`) and `birthtime_other.go` (`//go:build !darwin && !windows`, returns false). Keep the existing explanatory comments (the photo keeps the day it was taken, so it is dated by birth time and not by the last write) on `Created`/the per-OS files; the darwin and windows files need no build tag because the filename suffix already constrains them, exactly as today. Move `internal/core/imaging/file_created_test.go` to `internal/core/filedate/created_test.go` (package `filedate`, calling `Created`), keeping both cases (a freshly written file is dated now; a missing file has no date). Delete the three `internal/core/imaging/file_created_*.go` files, make `withFileDate` in `internal/core/imaging/exif_tags.go` call `filedate.Created`, and update the direct `fileCreated(path)` assertions in `internal/core/imaging/exif_tags_test.go` (~line 61) and `internal/core/imaging/orientation_test.go` (~lines 549, 565) to `filedate.Created(path)`. Add the `filedate/` line to the layout tree in CLAUDE.md. Verify with `go build ./...`, `go test ./internal/core/...` and a Windows cross-build (`CC=x86_64-w64-mingw32-gcc CGO_ENABLED=1 GOOS=windows GOARCH=amd64 go build ./...`).",
            "files": [
              "internal/core/filedate/created.go",
              "internal/core/filedate/birthtime_darwin.go",
              "internal/core/filedate/birthtime_windows.go",
              "internal/core/filedate/birthtime_other.go",
              "internal/core/filedate/created_test.go",
              "internal/core/imaging/file_created_darwin.go",
              "internal/core/imaging/file_created_windows.go",
              "internal/core/imaging/file_created_other.go",
              "internal/core/imaging/file_created_test.go",
              "internal/core/imaging/exif_tags.go",
              "internal/core/imaging/exif_tags_test.go",
              "internal/core/imaging/orientation_test.go",
              "CLAUDE.md"
            ]
          },
          {
            "title": "Extract native window maximizing into internal/gui/nativewin",
            "details": "Create `internal/gui/nativewin` and move `internal/gui/ui/maximize_darwin.go`, `maximize_darwin.m`, `maximize_windows.go` and `maximize_unix.go` into it unchanged except for the package clause, renaming the entry point `maximizeWindow` -> exported `Maximize(window fyne.Window)` in all three variants. The `.m` file must move together with the darwin `.go` file (it keeps its `//go:build darwin` line) or the cgo call to `maximizeNSWindow` fails to link. `screen.go` and `screen_test.go` move into the same package because `maximize_unix.go` depends on `screenLayout`, `screenMetrics`, `maximizedContentSize` and `primaryVideoMode`; `MonitorSize` stays exported and becomes `nativewin.MonitorSize`, everything else stays unexported. Update `internal/gui/ui/window.go` (`MainWindow.Maximize` calls `nativewin.Maximize(mw.window)`) and `internal/gui/app/app.go` (~line 118, the `fullImageSize` once-value now calls `nativewin.MonitorSize()` and imports `photo/internal/gui/nativewin`). `screen_test.go` moves as is apart from its package clause, so the `maximizedContentSize`/`centrableHeight` table tests keep running. Add the `nativewin/` entry under `gui/` in the CLAUDE.md layout tree. The `!windows && !darwin` variant is only compiled by the ubuntu CI job, so re-read `maximize_unix.go` after the move to confirm every symbol it uses now lives in the same package; check darwin locally with `go build ./...` and `go vet ./...`, and Windows with the mingw cross-build.",
            "files": [
              "internal/gui/nativewin/maximize_darwin.go",
              "internal/gui/nativewin/maximize_darwin.m",
              "internal/gui/nativewin/maximize_windows.go",
              "internal/gui/nativewin/maximize_unix.go",
              "internal/gui/nativewin/screen.go",
              "internal/gui/nativewin/screen_test.go",
              "internal/gui/ui/maximize_darwin.go",
              "internal/gui/ui/maximize_darwin.m",
              "internal/gui/ui/maximize_windows.go",
              "internal/gui/ui/maximize_unix.go",
              "internal/gui/ui/screen.go",
              "internal/gui/ui/screen_test.go",
              "internal/gui/ui/window.go",
              "internal/gui/app/app.go",
              "CLAUDE.md"
            ]
          },
          {
            "title": "Extract the claude CLI lookup into internal/core/claudebin",
            "details": "Move `internal/core/tags/claude_path.go` to `internal/core/claudebin/lookup.go` (package `claudebin`), renaming `LookupClaude` -> `Lookup` and `ErrClaudeNotFound` -> `ErrNotFound` to avoid stutter and keeping the unexported `lookupClaude`, `claudeBinaries`, `claudeDirs`, `claudeCandidates`, `isExecutable` helpers and the `osWindows` constant. Keep the `runtime.GOOS` switches rather than splitting into build-tagged files, so the Windows candidate list stays covered by the tests on a macOS host; the OS knowledge is now confined to one single-purpose package. Move `claude_path_test.go` to `internal/core/claudebin/lookup_test.go` with the renames applied. Update `internal/core/tags/tagger.go` (`NewTagger` uses `claudebin.Lookup`, the `//nolint:gosec` comment on `exec.CommandContext` mentions the new name) and the call sites of the error: `internal/core/tags/tagger_test.go` (~lines 111, 117 use `claudebin.ErrNotFound`; ~line 338 loses the `osWindows` constant and must compare `runtime.GOOS` against the literal `\"windows\"`), `internal/gui/ui/tags_dialog.go` (~line 314) and `internal/gui/ui/tags_dialog_test.go` (~line 173), which now import `photo/internal/core/claudebin` instead of relying on `tags`. Add the `claudebin/` line to the CLAUDE.md layout tree. Run `go test ./...` and `go tool golangci-lint run`.",
            "files": [
              "internal/core/claudebin/lookup.go",
              "internal/core/claudebin/lookup_test.go",
              "internal/core/tags/claude_path.go",
              "internal/core/tags/claude_path_test.go",
              "internal/core/tags/tagger.go",
              "internal/core/tags/tagger_test.go",
              "internal/gui/ui/tags_dialog.go",
              "internal/gui/ui/tags_dialog_test.go",
              "CLAUDE.md"
            ]
          }
        ],
        "questions": [
          {
            "question": "Package naming: assume `internal/core/filedate` with `filedate.Created(path)`, `internal/gui/nativewin` with `nativewin.Maximize`/`nativewin.MonitorSize`, and `internal/core/claudebin` with `claudebin.Lookup`/`claudebin.ErrNotFound` unless you prefer other names (affects all three steps).",
            "answer": null
          },
          {
            "question": "Step 1 restructures `fileCreated` into a shared `Created` plus a per-OS `birthTime(info)` instead of three near-identical copies of the stat-and-fallback code. Assume yes; say so if you want a literal 1:1 move of the three files.",
            "answer": null
          },
          {
            "question": "Step 2 moves `screen.go`/`screen_test.go` out of `internal/gui/ui` as well (they are not OS-specific but `maximize_unix.go` needs them), so `ui.MonitorSize` becomes `nativewin.MonitorSize` and `internal/gui/app/app.go` changes. Assume yes; the alternative is keeping a one-line `ui.MonitorSize` wrapper so `app.go` is untouched.",
            "answer": null
          },
          {
            "question": "Step 3 keeps `runtime.GOOS` branching inside the new `claudebin` package rather than converting it to `_windows.go`/`_other.go` files, because build tags would make the Windows path untestable on the dev machine and CI (both non-Windows). Assume yes; say so if you want build-tagged files with per-OS tests like `internal/core/proc`.",
            "answer": null
          }
        ],
        "risks": [
          "The darwin maximize is cgo: `maximize_darwin.m` must move together with `maximize_darwin.go` (and keep its `//go:build darwin` line), otherwise the macOS build fails to link `maximizeNSWindow`.",
          "The `!darwin && !windows` variants (`birthtime_other.go`, `maximize_unix.go`) are not compiled on the developer's macOS host; only the ubuntu CI job builds them, so a missed symbol in the moved `maximize_unix.go` will only surface in CI.",
          "The Windows-only files (`birthtime_windows.go`, `maximize_windows.go`) likewise only compile under `GOOS=windows`; the mingw cross-build must be run for each step, and `fyne package --release --os windows` bumps the build number in FyneApp.toml as a side effect, so prefer plain `GOOS=windows go build ./...` for verification.",
          "Renaming `ErrClaudeNotFound` -> `claudebin.ErrNotFound` touches the GUI (`tags_dialog.go`); the `errors.Is` check must keep pointing at the same sentinel or the \"claude CLI not found\" branch of the tags dialog silently stops matching.",
          "`tagger_test.go` borrows the `osWindows` constant from `claude_path.go`; if the constant leaves the package without that test being updated, the `tags` package stops compiling.",
          "CLAUDE.md documents the package layout and is loaded as project context; leaving it stale is a real (if non-breaking) regression, so each step should update the tree it changes."
        ]
      }
    },
    {
      "id": 6,
      "description": "Fix the unbounded widget map leak in the file browser and grid viewer by replacing the identity maps with a custom item widget type carrying its own state",
      "context": "On branch todo/4-simplify-code a review found that FileBrowser.items (internal/gui/ui/filebrowser.go, populated/read in internal/gui/ui/browseritem.go createItem/updateItem) and GridViewer.items (internal/gui/ui/gridviewer.go) are maps keyed by the root fyne.CanvasObject of each row/tile; since Fyne's List/GridWrap recreates item canvas objects on Refresh, each Refresh leaves a stale entry and the maps grow without bound. The agreed direction is to replace the identity map with a custom list/grid item widget type that carries its own state (thumb, name, star, dots, dateText for the browser; thumb, name for the grid). Row rendering for the file browser already lives in internal/gui/ui/browseritem.go with tests in internal/gui/ui/browseritem_test.go; the grid has no dedicated test file yet.",
      "docs": [
        "internal/gui/ui/browseritem.go",
        "internal/gui/ui/browseritem_test.go",
        "internal/gui/ui/filebrowser.go",
        "internal/gui/ui/gridviewer.go",
        "internal/gui/ui/photolist.go",
        "internal/gui/ui/photolist_test.go"
      ],
      "depends_on": [],
      "status": "open",
      "branch": null,
      "created_at": "2026-08-24T14:09:07Z",
      "updated_at": "2026-08-24T14:15:59Z",
      "plan": {
        "created_at": "2026-08-24T14:15:59Z",
        "revised_at": null,
        "base_commit": "a1d5806",
        "summary": "Both list-style views keep a `map[fyne.CanvasObject]*item` that is written in `createItem` and never pruned; because Fyne's `listRenderer.Refresh`/`gridWrapRenderer.Refresh` call `CreateItem` again (once for `itemMin`, plus drained item pool), every `Refresh` adds entries that are never removed, so the maps grow without bound and pin canvas objects and their thumbnails alive. The fix is to turn the per-row/per-tile state holders into real Fyne widgets that embed `widget.BaseWidget`, hold their own children, and expose an `update(...)` method; `createItem` then returns the widget itself and `updateItem` recovers the state with a type assertion instead of a map lookup, letting the widget (and its state) be garbage-collected with the canvas object. Step 1 does the file browser row (`browserItem`, `internal/gui/ui/browseritem.go` + `filebrowser.go`), step 2 does the grid tile (`gridItem`, moved out of `gridviewer.go` into a new `griditem.go`). Each step also extends/creates the matching test file so the reuse behaviour (state carried by the widget, stale values cleared when a row is reused for another photo) is covered. Existing free helpers (`updateThumbnail`, `updateStar`, `updateColorDots`, `updateDateText`) stay as functions so the current unit tests keep working.",
        "steps": [
          {
            "title": "Turn the file browser row into a self-contained widget",
            "details": "In `internal/gui/ui/browseritem.go` change `browserItem` to embed `widget.BaseWidget` and keep its existing children (`thumb`, `name`, `star`, `dots`, `dateText`) plus the assembled `root fyne.CanvasObject`. Add `newBrowserItem() *browserItem` that builds exactly the object tree that `FileBrowser.createItem` builds today (thumb with min size `thumbnailWidth`x`thumbnailHeight` and `ImageFillContain`, truncating name label, hidden star, `newColorDots()`, date text in the padded layout, the border/stack root) and calls `ExtendBaseWidget(item)` before returning; add `func (bi *browserItem) CreateRenderer() fyne.WidgetRenderer { return widget.NewSimpleRenderer(bi.root) }`. Add `func (bi *browserItem) update(photo model.Photo, meta model.PhotoMeta, thumbnail image.Image)` containing the body of the current `updateItem` after the data lookup (updateThumbnail/SetText/updateStar/updateColorDots/updateDateText). Keep the four free helper functions unchanged so the existing tests still compile. Rewrite `FileBrowser.createItem` to `return newBrowserItem()` and `FileBrowser.updateItem` to fetch the data via `fb.data.itemAt(id)`, resolve the thumbnail (`meta.Thumbnail` else `fb.imageProvider.Thumbnail(photo.ImagePath)`), then `item, ok := obj.(*browserItem); if !ok { return }` and `item.update(...)`. In `internal/gui/ui/filebrowser.go` delete the `items map[fyne.CanvasObject]*browserItem` field (line ~54) and its initialisation in `NewFileBrowser` (line ~71). In `internal/gui/ui/browseritem_test.go` add a test (no `t.Parallel`, using `test.NewTempWindow(t, item)` from `fyne.io/fyne/v2/test`, matching the driver-sharing note in `tags_dialog_test.go`) that creates one `browserItem`, calls `update` with a favourite photo carrying two colours and a date, asserts star/dots/date/name, then calls `update` again with a plain photo and asserts the star is hidden, the extra dots are hidden, the date text is empty and the name changed - i.e. the state now travels with the widget and no map is consulted.",
            "files": [
              "internal/gui/ui/browseritem.go",
              "internal/gui/ui/filebrowser.go",
              "internal/gui/ui/browseritem_test.go"
            ]
          },
          {
            "title": "Turn the grid tile into a self-contained widget and drop the grid map",
            "details": "Create `internal/gui/ui/griditem.go` holding `type gridItem struct { widget.BaseWidget; thumb *canvas.Image; name *widget.Label; root fyne.CanvasObject }`, a `newGridItem(tileWidth float32) *gridItem` that reproduces today's `GridViewer.createItem` body (thumb min size `tileWidth` x `tileWidth*gridThumbRatio`, `ImageFillContain`, centred truncating label, `container.NewBorder(nil, name, nil, nil, thumb)`) and calls `ExtendBaseWidget`, a `CreateRenderer` returning `widget.NewSimpleRenderer(gi.root)`, and `func (gi *gridItem) update(name string, img image.Image)` that calls `updateThumbnail(gi.thumb, img)` (the helper in browseritem.go is behaviourally identical to the grid's inline code) and `gi.name.SetText(name)`. In `internal/gui/ui/gridviewer.go` remove the `items` map field and its initialisation in `NewGridViewer`, remove the `gridItem` struct, make `createItem` read `tileWidth` under the mutex and `return newGridItem(tw)`, and rewrite `updateItem` to keep the existing locking and visibleMin/visibleMax bookkeeping but pick the image first (`gv.imageProvider.Peek(...)`, else `meta.Thumbnail` + `gv.schedulePreload()`, else nil + `gv.schedulePreload()`), then `item, ok := obj.(*gridItem); if !ok { return }` and `item.update(photo.Name, img)`; drop the now-unused `canvas` import if nothing else in the file uses it. Add `internal/gui/ui/griditem_test.go` (non-parallel, `test.NewTempWindow(t, item)`) asserting that a single `gridItem` reused for two photos shows the thumbnail and name of the second one and that passing a nil image hides the thumb and clears nothing else - covering the tile reuse path the grid has no test for today.",
            "files": [
              "internal/gui/ui/griditem.go",
              "internal/gui/ui/gridviewer.go",
              "internal/gui/ui/griditem_test.go"
            ]
          }
        ],
        "questions": [
          {
            "question": "Should `gridItem` live in a new `internal/gui/ui/griditem.go` (symmetric with `browseritem.go`) or stay inside `gridviewer.go`? Assume the new file unless told otherwise.",
            "answer": null
          },
          {
            "question": "The grid tile currently reuses `updateThumbnail` from `browseritem.go`, which is a small overlap with todo #7 (dedup thumbnail loading). Is that acceptable now, or should the grid keep its own inline image code until #7 lands? Assume reuse is fine.",
            "answer": null
          },
          {
            "question": "The tile thumbnail min size is set only at creation time (a resize relies on `grid.Refresh()` recreating items). Should `gridItem.update` also re-apply the current tile size? Assume no - keep today's behaviour, no functional change in this todo.",
            "answer": null
          },
          {
            "question": "New tests instantiate real widgets and therefore need the Fyne test driver (`test.NewTempWindow`) and must not use `t.Parallel`, unlike the current pure-helper tests in `browseritem_test.go`. Assume that mixed style is acceptable.",
            "answer": null
          }
        ],
        "risks": [
          "Row/tile sizing: `List`/`GridWrap` derive `itemMin` from `CreateItem().MinSize()`. `widget.NewSimpleRenderer` delegates MinSize to the wrapped root, so sizes should be identical, but a wrong `ExtendBaseWidget` call (missing, or before children are assigned) yields a zero MinSize and collapsed rows - verify visually or via a MinSize assertion in the tests.",
          "`createItemAndApplyThemeScope` in Fyne wraps the created object for theme scoping; custom widgets are supported, but each created widget now takes an entry in Fyne's internal renderer cache. That cache is expiry-driven and bounded, unlike the maps being removed, so this is a fix rather than a new leak - still worth a quick memory sanity check on a large folder.",
          "`FileBrowser.updateItem`/`GridViewer.updateItem` silently return when the type assertion fails; if any future caller passes a different object type the row will render stale content with no error, same failure mode as the old map miss.",
          "Touches files also under change on branch `todo/4-simplify-code` (`browseritem.go`, `filebrowser.go`) and overlaps with the future todo #7, so conflicts are likely if #7 starts before this lands."
        ]
      }
    },
    {
      "id": 7,
      "description": "Deduplicate the thumbnail loading logic between the grid viewer and the file browser row updateThumbnail",
      "context": "During the code-simplification pass on branch todo/4-simplify-code, file-browser row rendering (including the shared updateThumbnail helper that sets/hides a *canvas.Image) was extracted into internal/gui/ui/browseritem.go with tests in browseritem_test.go; review flagged that GridViewer.updateItem in internal/gui/ui/gridviewer.go repeats essentially the same thumbnail resolve-and-apply flow (provider Peek/Thumbnail fallback to meta.Thumbnail, else hide, plus async preload refresh) and the deduplication was deliberately deferred to this todo. Both paths read from the same thumbnail cache in internal/core/imaging/image_provider.go (Peek/Get/Preload/Thumbnail/Gen), but differ in that the grid is size-aware (thumbPixelSize) and schedules preloads while the browser row does not. There are no tests for gridviewer.go or filebrowser.go, so browseritem_test.go is the only existing coverage for the shared helper.",
      "docs": [
        "internal/gui/ui/browseritem.go",
        "internal/gui/ui/browseritem_test.go",
        "internal/gui/ui/gridviewer.go",
        "internal/gui/ui/filebrowser.go",
        "internal/core/imaging/image_provider.go",
        "internal/core/imaging/image_provider_test.go"
      ],
      "depends_on": [
        4
      ],
      "status": "open",
      "branch": null,
      "created_at": "2026-08-24T14:09:07Z",
      "updated_at": "2026-08-24T14:16:38Z",
      "plan": {
        "created_at": "2026-08-24T14:16:38Z",
        "revised_at": null,
        "base_commit": "a1d5806",
        "summary": "GridViewer.updateItem and the file-browser row both resolve a thumbnail out of imaging.Provider and push it onto a *canvas.Image, differing only in that the grid asks for a size-aware image (Peek(path, thumbPixelSize)) and schedules a background preload when the sized image is not cached yet, while the row asks only for the small cached thumbnail (Provider.Thumbnail) and never preloads. The plan extracts one helper into a new internal/gui/ui/thumbnail.go: a narrow thumbnailSource interface (Peek + Thumbnail, satisfied by *imaging.Provider and by a fake in tests), a resolve step that returns the best in-memory image plus a needsLoad flag, and the existing updateThumbnail show/hide/refresh step, wrapped in applyThumbnail(thumb, src, path, embedded, size) (needsLoad bool). The browser passes size <= 0 and ignores the flag; the grid passes thumbPixelSize() and calls schedulePreload() when needsLoad is true, so the preload policy and the size-awareness stay where they belong. Note that Provider.Peek(path, 0) is NOT equivalent to Provider.Thumbnail(path) (loader.Peek clamps size to 1 and would hand back a full-resolution decoded image for a 48x36 row), so the unsized path must keep calling Thumbnail. The only intended behaviour change is that the browser row now prefers the provider's cached thumbnail over meta.Thumbnail, matching the grid; both images are orientation-corrected and at most 160px, so the row looks the same.",
        "steps": [
          {
            "title": "Extract the shared thumbnail resolve-and-apply helper and use it in the browser row",
            "details": "Create internal/gui/ui/thumbnail.go holding: (1) `type thumbnailSource interface { Peek(path string, size int) image.Image; Thumbnail(path string) image.Image }`, which *imaging.Provider already satisfies; (2) `updateThumbnail(thumb *canvas.Image, img image.Image)` moved verbatim out of browseritem.go (set Image + Show, or nil + Hide, then Refresh); (3) `applyThumbnail(thumb *canvas.Image, src thumbnailSource, path string, embedded image.Image, size int) (needsLoad bool)` which does: if size > 0 and src.Peek(path, size) != nil -> apply it, return false; if size <= 0 and src.Thumbnail(path) != nil -> apply it, return false; otherwise apply `embedded` (may be nil, which hides the widget) and return true. Keep a short comment saying why size <= 0 must not go through Peek: Loader.Peek clamps the size to 1 and would return the full-resolution cached image, which the 48x36 row must never hold. Then change FileBrowser.updateItem in internal/gui/ui/browseritem.go to a single `applyThumbnail(item.thumb, fb.imageProvider, photo.ImagePath, meta.Thumbnail, 0)` call, dropping the local `thumbnail` variable and the direct Provider.Thumbnail call; remove updateThumbnail and the now-unused `image` import from browseritem.go. Add internal/gui/ui/thumbnail_test.go with a fake thumbnailSource (records the sizes it was asked for, returns canned images) covering: sized hit -> that image shown, needsLoad false; sized miss with embedded -> embedded shown, needsLoad true; sized miss without embedded -> Image nil, widget hidden, needsLoad true; unsized -> cached thumbnail wins over embedded and Peek is never called; and the reuse case where a widget that held an image is reset for a photo with none (stays hidden with a nil Image). Follow the existing style of browseritem_test.go (t.Parallel, testify require/assert, no fyne test app needed - canvas.Refresh is a no-op without a running app). Run `go test ./internal/gui/ui/...` and `go tool golangci-lint run`.",
            "files": [
              "internal/gui/ui/thumbnail.go",
              "internal/gui/ui/thumbnail_test.go",
              "internal/gui/ui/browseritem.go"
            ]
          },
          {
            "title": "Route GridViewer.updateItem through the shared helper",
            "details": "In internal/gui/ui/gridviewer.go replace the four-branch if/else in updateItem (lines ~163-176) with `if applyThumbnail(item.thumb, gv.imageProvider, photo.ImagePath, meta.Thumbnail, gv.thumbPixelSize()) { gv.schedulePreload() }`, keeping thumbPixelSize() called outside gv.mu as it is today (it takes the mutex itself). Leave the name label update, the visibleMin/visibleMax bookkeeping and schedulePreload untouched. Double-check the preload trigger is unchanged: today a preload is scheduled whenever the sized Peek missed, whether or not the embedded thumbnail filled in - the helper's needsLoad must be true in both of those cases and false only on a Peek hit, otherwise tiles either stay at embedded-thumbnail quality forever or the preload goroutine is re-armed on every visible tile. Extend internal/gui/ui/thumbnail_test.go with an explicit assertion for the grid-shaped contract (size > 0, embedded present, Peek miss -> needsLoad true) if step 1 did not already name that case. Verify the canvas.Image drops its old image when a tile is reused for a photo with nothing cached. Run `go test ./internal/gui/ui/...` and `go tool golangci-lint run`; then, if the app can be launched, open a folder and toggle grid view (L) to confirm tiles still sharpen as preloads land and the left-hand rows still show thumbnails.",
            "files": [
              "internal/gui/ui/gridviewer.go",
              "internal/gui/ui/thumbnail_test.go"
            ]
          },
          {
            "title": "Share the thumbnail canvas.Image constructor between the two createItem functions",
            "details": "Both FileBrowser.createItem (internal/gui/ui/browseritem.go) and GridViewer.createItem (internal/gui/ui/gridviewer.go) build the same widget: canvas.NewImageFromImage(nil) + SetMinSize(w, h) + FillMode = canvas.ImageFillContain. Add `newThumbnailImage(width, height float32) *canvas.Image` to internal/gui/ui/thumbnail.go and call it from both, with the browser passing thumbnailWidth/thumbnailHeight and the grid passing tw and tw*gridThumbRatio. Pure refactor, no behaviour change; a one-line test in thumbnail_test.go asserting the min size and fill mode is enough. Skip this step if the answer to the corresponding question is no. Run `go test ./internal/gui/ui/...` and `go tool golangci-lint run`.",
            "files": [
              "internal/gui/ui/thumbnail.go",
              "internal/gui/ui/browseritem.go",
              "internal/gui/ui/gridviewer.go",
              "internal/gui/ui/thumbnail_test.go"
            ]
          }
        ],
        "questions": [
          {
            "question": "The file-browser row currently prefers meta.Thumbnail and only then the provider cache, while the grid prefers the cache. Unifying on cache-first means a row can show the decoded downscaled thumbnail (<=160px, orientation-corrected) instead of the embedded EXIF one - visually the same at 48x36. Assume cache-first for both unless told otherwise; affects step 1.",
            "answer": null
          },
          {
            "question": "Is a single applyThumbnail with `size <= 0` meaning 'small cached thumbnail only' acceptable, or should there be two named functions (e.g. applySizedThumbnail / applyRowThumbnail)? Assume the single function with the sentinel; affects steps 1 and 2.",
            "answer": null
          },
          {
            "question": "Should the helper stay in internal/gui/ui behind a small thumbnailSource interface, or should imaging.Provider grow a resolve method (core may not import Fyne, so only the image resolution could move)? Assume it stays in internal/gui/ui; affects step 1.",
            "answer": null
          },
          {
            "question": "Is step 3 (sharing newThumbnailImage between the two createItem functions) in scope for this todo, or should it be left to todo #6, which rewrites both item constructors anyway? Assume in scope unless told otherwise.",
            "answer": null
          },
          {
            "question": "browseritem.go exists only on branch todo/4-simplify-code, not on main. Should this todo's branch be cut from todo/4-simplify-code (or should #4 be merged first)? Assume branching from todo/4-simplify-code; affects every step.",
            "answer": null
          }
        ],
        "risks": [
          "Hard prerequisite: internal/gui/ui/browseritem.go and the current shape of updateItem exist only on branch todo/4-simplify-code; branching from main would make every step unbuildable.",
          "Overlap with todo #6, which replaces the fb.items / gv.items identity maps with item widget types and rewrites the same two updateItem bodies - whichever lands second will have to rebase over the other.",
          "Getting the needsLoad flag wrong silently breaks the grid: always false means tiles never sharpen past the embedded thumbnail; always true re-arms the preload goroutine on every visible tile (extra CPU and repeated RefreshItem calls).",
          "Provider.Peek(path, 0) is not a drop-in for Provider.Thumbnail(path) - loader.Peek clamps the size to 1 and returns any cached full-resolution image, so routing the browser row through Peek would put a full-size image into a 48x36 canvas.Image and rescale it on every draw.",
          "There are no tests for gridviewer.go or filebrowser.go, so the grid change is only covered indirectly through the helper's tests; the preload/refresh behaviour has to be confirmed by running the app."
        ]
      }
    }
  ]
}
